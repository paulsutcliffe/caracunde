= form_for(@album) do |f|

  - if @album.errors.any?
    #errorExplanation
      %h2= t('activerecord.errors.messages.album.invalid_record')
      %ul
      - @album.errors.full_messages.each do |msg|
        %li= msg
  .field
    = f.label :nome
    %br
    = f.text_field :nome

  .actions
    = f.submit I18n.t('submit')
