- form_for(@patrocinador) do |f|
  
  - if @patrocinador.errors.any?
    #errorExplanation
      %h2= t('activerecord.errors.messages.patrocinador.invalid_record')
      %ul
      - @patrocinador.errors.full_messages.each do |msg|
        %li= msg
  .field
    = f.label :link
    %br
    = f.text_field :link

  .actions
    = f.submit I18n.t('submit')
