- form_for(@slide) do |f|
  
  - if @slide.errors.any?
    #errorExplanation
      %h2= t('activerecord.errors.messages.slide.invalid_record')
      %ul
      - @slide.errors.full_messages.each do |msg|
        %li= msg

  .actions
    = f.submit I18n.t('submit')
